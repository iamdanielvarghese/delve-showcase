{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Building Infinite Worlds: Procedural Terrain Generation","text":"<p>Executive Summary</p> <p>Delve is a custom Python-based exploration engine. The core objective was to move away from static, hand-drawn levels and build an architecture capable of generating infinite, playable terrain dynamically using Perlin Noise algorithms, rendering natively via Pygame, and caching via SQLite.</p>"},{"location":"#system-architecture","title":"System Architecture","text":"<p>To create natural geography rather than chaotic \"TV static,\" the engine relies on procedural mathematical generation. </p> The Algorithm (Perlin Noise)Data QuantizationCaching (SQLite) <p>Unlike standard pseudo-random number generators, Perlin Noise calculates smooth, continuous gradients. By layering multiple \"octaves\" of noise and adjusting the frequency and amplitude, the algorithm generates a mathematical heightmap.</p> <p>This continuous mathematical output is quantized into discrete grid tiles. The engine maps specific height thresholds to distinct biomes (e.g., deep water, sand, grass, rock) on the fly.</p> <p>To prevent memory overflow, generated chunks are saved to an SQLite database. When a player leaves an area and returns, the engine reads the cached database chunk rather than recalculating the Perlin math.</p>"},{"location":"#engineering-bottleneck-dynamic-collision","title":"Engineering Bottleneck: Dynamic Collision","text":"<p>The Obstacle</p> <p>Standard collision detection relies on static hitboxes drawn in the engine. Because Delve generates terrain infinitely, the environment is fundamentally dynamic. The core challenge was mapping the player's continuous pixel coordinates to the discrete, procedurally generated grid arrays in real-time. If the math was slightly off, the sprite would phase directly through generated mountains.</p> <p>The Solution</p> <p>Instead of relying on brute-force hitbox overlaps, I implemented a predictive grid-mapping system. Before committing to a movement frame, the engine calculates the player's intended next position, translates it into a grid index, and queries the generated <code>biomes</code> array.</p>"},{"location":"#core-implementation","title":"Core Implementation","text":"<p>Here is the production logic handling the predictive collision. Notice how the movement vector is blocked before the frame ever renders if the target tile is impassable.</p> <pre><code># Calculate intended movement vector\n# new_x, new_y = current_x + dx, current_y + dy\n\n# Predictive Grid-Mapping &amp; Collision Check\nif 0 &lt;= new_x &lt; MAP_W and 0 &lt;= new_y &lt; MAP_H:\n    # Query the dynamically generated Perlin noise array\n    if biomes[new_y, new_x] == TYPE_FLOOR:\n        # Commit movement only if the target tile is passable\n        px, py = new_x, new_y\n</code></pre>"},{"location":"#tech-stack","title":"Tech Stack","text":"<ul> <li>Language: Python</li> <li>Rendering: Pygame</li> <li>Data Storage: SQLite (for chunk caching)</li> <li>Algorithms: Perlin Noise (Procedural Generation)</li> </ul>"}]}